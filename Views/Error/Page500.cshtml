
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <title>500 - Sunucu Hatası</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <style>
        body {
            background: #fff5f5;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            font-family: 'Segoe UI', sans-serif;
            margin: 0;
        }

        .error-card {
            text-align: center;
            max-width: 550px;
            padding: 40px;
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(220, 53, 69, 0.1); 
        }

        .icon-box {
            font-size: 80px;
            color: #dc3545;
            margin-bottom: 20px;
            animation: spin 10s linear infinite;
        }

        .error-code {
            font-size: 80px;
            font-weight: 800;
            color: #dc3545;
            letter-spacing: -2px;
            line-height: 1;
        }

        .error-title {
            font-size: 26px;
            margin-top: 10px;
            font-weight: 700;
            color: #343a40;
        }

        .error-desc {
            color: #6c757d;
            margin-top: 15px;
            margin-bottom: 30px;
            font-size: 16px;
            line-height: 1.6;
        }

        .btn {
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

            .btn:hover {
                transform: translateY(-3px);
                box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            }

        @@keyframes spin {
            100% {
                transform: rotate(360deg);
            }
        }
    </style>
</head>
<body>
    <div class="error-card">
        <div class="icon-box">
            <i class="fas fa-cogs"></i>
        </div>
        <div class="error-code">500</div>
        <h1 class="error-title">Beklenmedik Bir Hata!</h1>
        <p class="error-desc">
            Sunucularımızda teknik bir sorun oluştu. Bu durum sizinle ilgili değil.<br />
            Teknik ekibimiz şu an konuyla ilgileniyor.
        </p>

        <div class="d-flex justify-content-center gap-2">
            <button onclick="goBackOrHome()" class="btn btn-outline-secondary rounded-pill px-4">
                <i class="fas fa-arrow-left me-2"></i>Geri Dön
            </button>

            <a href="/Default/Index" class="btn btn-danger rounded-pill px-4">
                <i class="fas fa-home me-2"></i>Anasayfa
            </a>
        </div>
    </div>

    <script>
        function goBackOrHome() {
            if (document.referrer === "" || window.history.length <= 1) {
                window.location.href = "/Default/Index";
            } else {
                window.history.back();
            }
        }
    </script>
</body>
</html>